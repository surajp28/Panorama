syntax = "proto3";
package auth;


service Auth {
    rpc basic (basicCreds) returns (authResult) {}
}

message basicCreds {
    string username = 1;
    string password = 2;
}

message authResult {
    bool success = 4;
    string token = 3;
}